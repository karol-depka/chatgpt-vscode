{
  "name": "my-ts-cli",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "rm -rf dist && ts-node src/index_openai.ts",
    "test": "rm -rf dist && jest",
    "prepublish": "yarn build"
  },
  "keywords": [],
  "author": "karol",
  "license": "AGPL-3.0-only",
  "dependencies": {
    "@types/node": "^version",
    "axios": "^1.6.0",
    "chalk": "^4.1.2",
    "dotenv": "^16.3.1",
    "inquirer": "^9.2.11",
    "marked": "^9.1.5",
    "openai": "^4.14.2",
    "ts-node": "^version",
    "typescript": "^version"
  },
  "devDependencies": {
    "@types/jest": "^latest",
    "jest": "^latest",
    "ts-jest": "^latest"
  },
  "bin": {
    "my-cli": "./dist/index.js"
  }
}
